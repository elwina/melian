export interface MeasureSquareOptionsType {
  initmm: number;
  offsetmm: number;
}

export interface MeasureSquareStyleType {
  mm2px: number;

  upHeight: number;
  downHeight: number;
  dsHeight: number;
  fontHeight: number;
  sfontHeight: number;

  fontSize: number;
  sfontSize: number;
  lineWidth: number;
  leftPadding: number;
  upPadding: number;

  leftMargin: number;
  bottomMargin: number;
}

export interface MeasureCircleOptionsType {
  initmm: number;
  offsetmm: number;
}

export interface MeasureCircleStyleType {
  mm2px: number;
  hm2px: number;

  seeSize: number;
  bigHeight: number;
  leftPadding: number;

  mainLine: number;
  hLine: number;
  lineWidth: number;
  mFontSize: number;
  hFontSize: number;

  leftMargin: number;
  bottomMargin: number;
}
